"0","```r
OD_table <- OD_table %>% 
  left_join(transmute(st_drop_geometry(TTS_Dataframe.sf), # Drop the geometry, it makes `distinct` misbehave
                      Zone_Destination, 
                      X.Destination, 
                      Y.Destination) %>% 
              distinct(Zone_Destination, .keep_all = TRUE), 
            by = \"Zone_Destination\")
```"
